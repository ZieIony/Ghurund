<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Ghurund.Controls.Workspace">

	<Style TargetType="{x:Type local:PeekPanel}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:PeekPanel}">
					<Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
						<DockPanel>
							<StackPanel x:Name="leftPanel" DockPanel.Dock="Left" Margin="0,4,0,4"/>
							<StackPanel x:Name="rightPanel" DockPanel.Dock="Right" Margin="0,4,0,4"/>
							<StackPanel x:Name="topPanel" DockPanel.Dock="Top" Orientation="Horizontal" Margin="4,0,4,0"/>
							<StackPanel x:Name="bottomPanel" DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="4,0,4,0"/>
							<Grid>
								<local:SplitPanel x:Name="peekedContent" Visibility="Collapsed" Panel.ZIndex="1000" Margin="4"/>
								<ContentPresenter Margin="4"/>
							</Grid>
						</DockPanel>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
