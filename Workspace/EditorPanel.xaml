<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Ghurund.Controls.Workspace">

	<Style TargetType="{x:Type local:EditorPanel}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:EditorPanel}">
					<Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
						<DockPanel Background="{TemplateBinding Background}">
							<local:TitleBar x:Name="titleBar"
											Title="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Title.Long}"
											Icon="{TemplateBinding Icon}" IsEnabled="{TemplateBinding IsEnabled}"
											PanelFocused="{TemplateBinding PanelFocused}" DockPanel.Dock="Top"
											MinimizeButtonVisibility="Visible"/>
							<ContentPresenter ContentSource="Content" IsEnabled="{TemplateBinding IsEnabled}"/>
						</DockPanel>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
